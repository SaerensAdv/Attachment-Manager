import{describe,expect,it}from"vitest";import type{RunIndexFilters}from"./runs-index";describe("Runs index contract",()=>{it("keeps filters explicit and bounded",()=>{const filters:RunIndexFilters={query:"audit",status:"failed",trigger:"scheduled",page:2,limit:100};expect(filters).toMatchObject({page:2,limit:100})});it("models archive separately from execution status",()=>{const archived={status:"completed",archivedAt:"2026-07-19T00:00:00Z"};expect(archived.status).toBe("completed");expect(archived.archivedAt).not.toBeNull()})});
