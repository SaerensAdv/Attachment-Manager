import{Router,type IRouter}from"express";import{archiveRun,getRunDeliveryEvents,listRunsPage}from"../lib/runs-index";import{recordActionEvent}from"../lib/action-events";import{actionResult,apiProblem}from"../lib/http-contract";const router:IRouter=Router();const number=(value:unknown,fallback:number)=>typeof value==="string"&&Number.isFinite(Number(value))?Number(value):fallback;
router.get("/runs/index",async(req,res)=>{res.json(await listRunsPage({query:typeof req.query.q==="string"?req.query.q:undefined,status:typeof req.query.status==="string"?req.query.status:undefined,client:typeof req.query.client==="string"?req.query.client:undefined,workflow:typeof req.query.workflow==="string"?req.query.workflow:undefined,trigger:typeof req.query.trigger==="string"?req.query.trigger:undefined,from:typeof req.query.from==="string"?req.query.from:undefined,to:typeof req.query.to==="string"?req.query.to:undefined,page:number(req.query.page,1),limit:number(req.query.limit,30),includeArchived:req.query.includeArchived==="true"})});
router.get("/runs/:id/delivery",async(req,res)=>{const id=Number(req.params.id);if(!Number.isInteger(id)||id<=0){res.status(400).json(apiProblem({error:"Invalid run id.",code:"INVALID_RUN_ID"}));return}res.json({events:await getRunDeliveryEvents(id)})});
router.post("/runs/:id/archive",async(req,res)=>{const id=Number(req.params.id);if(!Number.isInteger(id)||id<=0){res.status(400).json(apiProblem({error:"Invalid run id.",code:"INVALID_RUN_ID"}));return}if(!await archiveRun(id)){res.status(409).json(apiProblem({error:"Run is already archived or no longer exists.",code:"RUN_NOT_ARCHIVABLE"}));return}const result=actionResult({action:"run.archive",code:"RUN_ARCHIVED",message:"Run archived. Audit evidence was retained.",changed:true,verified:true,target:{id}});await recordActionEvent({action:result.action,targetType:"generation",targetId:id,nextState:{archived:true},providerResult:result});res.json(result)});export default router;
