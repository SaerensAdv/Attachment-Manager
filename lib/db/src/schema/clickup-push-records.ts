import { integer,pgTable,serial,text,timestamp } from "drizzle-orm/pg-core";
export const clickupPushRecordsTable=pgTable("clickup_push_records",{id:serial("id").primaryKey(),kind:text("kind").notNull(),idempotencyKey:text("idempotency_key").notNull().unique(),sourceRunId:text("source_run_id"),correlationId:text("correlation_id"),clickupObjectId:text("clickup_object_id"),clickupUrl:text("clickup_url"),status:text("status").notNull().default("pending"),attempts:integer("attempts").notNull().default(0),lastErrorCode:text("last_error_code"),nextAttemptAt:timestamp("next_attempt_at",{withTimezone:true}),terminalAt:timestamp("terminal_at",{withTimezone:true}),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull(),updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull()});
export type ClickupPushRecord=typeof clickupPushRecordsTable.$inferSelect;export type InsertClickupPushRecord=typeof clickupPushRecordsTable.$inferInsert;
