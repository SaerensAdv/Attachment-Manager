import{describe,expect,it}from"vitest";import{hashHeldSnapshot}from"./approval-governance";describe("approval governance",()=>{it("creates a stable immutable held-snapshot identity",()=>{expect(hashHeldSnapshot('{"kind":"email-reply","body":"hello"}')).toMatch(/^[a-f0-9]{64}$/);expect(hashHeldSnapshot("same")).toBe(hashHeldSnapshot("same"));expect(hashHeldSnapshot("one")).not.toBe(hashHeldSnapshot("two"))});it("does not invent evidence without a snapshot",()=>expect(hashHeldSnapshot(null)).toBeNull())});
