/**
 * Generated by orval v8.9.1 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface DocNode {
  /** Stable identifier, equal to the path relative to the docs root. */
  id: string;
  /** Path relative to the docs root (e.g. knowledge/tone-of-voice.md). */
  path: string;
  /** The document's first heading, or the filename if none. */
  title: string;
  /** One of core, agent, client, workflow, template, knowledge. */
  category: string;
  /**
     * First paragraph of the document, if any.
     * @nullable
     */
  summary: string | null;
}

export interface DocEdge {
  /** The id of the document the relationship starts from. */
  source: string;
  /** The id of the document the relationship points to. */
  target: string;
  /** The relationship type (reference or mention). */
  kind: string;
}

export interface DocCategory {
  id: string;
  label: string;
  count: number;
}

export interface DocGraph {
  nodes: DocNode[];
  edges: DocEdge[];
  categories: DocCategory[];
}

export interface DocContent {
  id: string;
  path: string;
  title: string;
  category: string;
  /** The raw markdown content of the document. */
  content: string;
}

export interface ErrorResponse {
  error: string;
}

export type GetDocContentParams = {
/**
 * The document path relative to the documentation root (e.g. agents/orchestrator.md).
 */
path: string;
};

