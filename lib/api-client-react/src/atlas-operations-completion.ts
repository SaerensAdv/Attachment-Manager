import { customFetch } from "./custom-fetch";export interface WebhookDeadLetter{id:number;eventType:string;taskId:string;attempts:number;lastError:string|null;receivedAt:string;terminalAt:string|null}export const getAtlasWebhookDeadLetters=(signal?:AbortSignal)=>customFetch<{records:WebhookDeadLetter[]}>("/api/clickup/webhooks/dead-letters",{method:"GET",responseType:"json",signal});
