import { Router,type IRouter } from "express";import { listAlerts,resolveAlert as resolveAlertRow,type SystemAlert } from "../lib/alerts-store";import { recordActionEvent } from "../lib/action-events";const router:IRouter=Router();const parseId=(raw:string)=>{const id=Number(raw);return Number.isInteger(id)&&id>0?id:null};export function serializeAlert(a:SystemAlert){return{id:a.id,source:a.source,severity:a.severity,message:a.message,context:a.context,occurrences:a.occurrences,firstSeenAt:a.firstSeenAt.toISOString(),lastSeenAt:a.lastSeenAt.toISOString(),resolvedAt:a.resolvedAt?a.resolvedAt.toISOString():null}}
router.get("/alerts",async(req,res)=>{const unresolvedOnly=req.query.unresolvedOnly==="true"||req.query.unresolvedOnly==="1";res.json({alerts:(await listAlerts({unresolvedOnly})).map(serializeAlert)})});router.post("/alerts/:id/resolve",async(req,res)=>{const id=parseId(req.params.id);if(id===null){res.status(400).json({error:"Ongeldige id."});return}const resolved=await resolveAlertRow(id);if(!resolved){res.status(404).json({error:"Melding niet gevonden of al opgelost."});return}await recordActionEvent({action:"alert.resolve",targetType:"system_alert",targetId:id,nextState:{resolvedAt:resolved.resolvedAt?.toISOString()??null}});res.json(serializeAlert(resolved))});export default router;
