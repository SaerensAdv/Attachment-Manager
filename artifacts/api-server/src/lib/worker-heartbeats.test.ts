import { describe, expect, it } from "vitest";
import { classifyHeartbeat, type WorkerHeartbeat } from "./worker-heartbeats";
const heartbeat = (overrides: Partial<WorkerHeartbeat> = {}): WorkerHeartbeat => ({ name: "scheduler", instanceId: "test", status: "healthy", heartbeatAt: "2026-07-19T12:00:00.000Z", startedAt: "2026-07-19T11:00:00.000Z", lastSuccessAt: "2026-07-19T12:00:00.000Z", lastErrorAt: null, lastErrorCode: null, metadata: {}, ...overrides });
describe("durable worker heartbeat truth", () => { it("never calls a missing worker healthy", () => expect(classifyHeartbeat(undefined).status).toBe("unknown")); it("marks stale workers down", () => expect(classifyHeartbeat(heartbeat(), Date.parse("2026-07-19T12:04:00.000Z")).message).toBe("HEARTBEAT_STALE")); it("preserves explicit worker failures", () => expect(classifyHeartbeat(heartbeat({ status: "failed", lastErrorCode: "TICK_FAILED" }), Date.parse("2026-07-19T12:00:01.000Z"))).toMatchObject({ status: "down", message: "TICK_FAILED" })); });
