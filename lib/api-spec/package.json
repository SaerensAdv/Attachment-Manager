{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare:atlas": "node ./prepare-atlas-openapi.mjs",
    "codegen": "pnpm run prepare:atlas && orval --config ./orval.config.ts && rm -f ./.atlas-openapi.generated.yaml && pnpm -w run typecheck:libs",
    "codegen:check": "pnpm run codegen"
  },
  "devDependencies": {
    "orval": "^8.9.1"
  }
}
